# 品牌背景候选与偏好排序工作台

## 目标

公共网站继续使用确定性的参数渲染，不调用生图或 AI API。模板发布者通过“保留 / 淘汰”积累本机选择记录，偶尔更新并导出一个很小的排序模型文件，再随网站版本发布。

排序模型只负责候选顺序，不负责像素生成，也不能绕过品牌色、文字安全区和跨尺寸一致性等硬规则。

## 当前试点范围

- 花卉区保留三个入口：`petal`（单片花瓣）和 `petalShadow`（花瓣叠影）严格复用 9.2v1 的原始着色程序；`petalStack`（微距花卉）使用新的参数化生成器与候选排序。
- 每批候选：6、8、10 或 12 组，默认 10 组。候选会强制包含不同内部结构，避免只出现同一张图的微调版。
- 模板参数：造花方法、花瓣层数、每层片数、层间透气、层间错位、疏密节奏、主次大小比、生长位置、裁切比例、非对称度、黄橙面积、展开度、远近虚实、尖圆度、弯曲度、开合度和边缘清晰度。近处轮廓明确，远处逐层虚化并散开；右下花心由花瓣根部聚合形成黄橙扩散，不绘制独立圆形。
- 发布模型：`gradient-preference-model.json`。
- 页面逻辑：`gradient-preference.js`。
- 实际像素渲染仍由 `gradient-field.js` 完成。

其他模板暂时不共用花瓣参数。验证选择机制后，液态、树影、笔触等模板需要分别定义自己的参数、约束和评分特征。

## 两种模式

### 使用模式

面向普通使用者。选择品牌、模板和场景后生成候选，直接点击候选卡片即可应用到上方大图，再沿用现有下载功能导出当前图或整套尺寸。

### 我的模板工作台

只面向模板发布者。普通模式会隐藏专属参数、排序分数、保留/淘汰与模型数据操作；候选卡仍直接叠加当前真实文案。工作台中可以展开完整参数，标记“保留”或“淘汰”并记录原因。选择记录保存在当前浏览器本机，不会上传。正式发布时应将工作台脚本与公共使用页分离，而不是只依靠界面隐藏作为权限控制。

不建议整批统一标记为保留或淘汰。偏好排序需要同一批中相对更好与相对更差的样本；每轮只标记少量最喜欢和最不喜欢的候选，信息量通常更高。

满足至少 2 个保留和 2 个淘汰样本后，可以点击“根据本机选择更新排序”。本机排序不会自动修改网站文件；需要导出排序模型，审核后再替换 `gradient-preference-model.json` 并作为新版本发布。

## 数据与版本

- 本机选择记录：浏览器 `localStorage`，键名 `capcut-gradient-preference-log-v1`。
- 本机排序覆盖：浏览器 `localStorage`，当前键名 `capcut-gradient-preference-local-model-v2`。旧的 v1 覆盖仍保留在浏览器中，但不会盖住这轮新的疏密节奏基础模型。
- 公开网站默认只读取仓库中的 `gradient-preference-model.json`。
- 清理浏览器数据会删除本机选择记录，因此重要记录应使用“导出选择记录”保存。
- 任何新排序模型在发布前都必须重新检查两个品牌、代表性横 / 竖 / 方画幅、文字可读性和导出。

## 当前排序方法

当前是可解释的线性排序试点。发布模型中的初始权重来自已经明确的设计反馈，例如避免平均分布、强调主次、保持边缘生长、限制暖色面积。点击本机更新后，页面用保留样本与淘汰样本之间的平均特征差更新权重。

这不是 LoRA，也不是大型图像模型训练。若后续数据量增加，可以替换成更正式的成对偏好排序，但页面输入输出和参数模板不需要推倒重做。

## 2026-09-03 人工偏好结论

- 两份导出文件是同一批记录的递增版本，第二份包含 22 次操作，其中有重复点击；它们不应被当作 33 个独立样本。
- 较强正样本共同指向“虚化散开、3 层、每层 3–4 片”。这是一种当前偏好，不是禁止另外两种造花方法。
- “太均匀”横跨三种造花方法，说明它首先是生成规则问题：即使非对称参数很高，等角度、近似等长、等透明度的排列仍会显得机械。
- 新增“疏密节奏”，并让它共同控制每层角度漂移、局部聚散、主瓣选择、长宽差、透明度差和虚化差。
- 对“多层叠瓣、3 层、每层 4 片”的反馈采用拆分记录：层次丰富是正向属性，重复节奏是负向属性，不把整张图简单视为全错。
